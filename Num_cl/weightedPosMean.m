
function weightedPosMean = weightedPosMean(data_matrix)
    weightedPosMean = zeros(size(data_matrix, 1), 1);
    weighted_row_sum = 0;
    for k = 1:size(data_matrix, 1)
        weighted_row_sum = weighted_row_sum +;
    end

end